---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by xueruixiang.
--- DateTime: 2021/10/22 18:58
---

local M = class("CastingSwordTaskInfoModel", LikeOO.OODataBase)

function M:onCreate()
    M.super.onCreate(self)
    self.m_allAwardsData = self.m_params.m_allAwardsData
    self.m_allPersonAwardsData = self.m_params.m_allPersonAwardsData
    self.m_score_done = self.m_params.m_score_done
    self.m_person_score_done = self.m_params.m_person_score_done
    self.m_score = self.m_params.m_score
    self.m_self_score = self.m_params.m_self_score
    self.m_version = self.m_params.m_version
    self:getData()
end

function M:setScoreDone(score_done)
    local tempData = {}
    for i = 1, #score_done do
        local index = score_done[i]
        tempData[index] = index
    end
    self.m_score_done = tempData
end

function M:setPersonScoreDone(person_score_done)
    local tempData = {}
    for i = 1, #person_score_done do
        local index = person_score_done[i]
        tempData[index] = index
    end
    self.m_person_score_done = tempData
end

return M